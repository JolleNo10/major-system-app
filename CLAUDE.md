# Mnemonics

Read `AGENTS.md`. It is the canonical working agreement for this repository —
architecture routing, global invariants, verification policy, and git workflow
— and it is tool-neutral, so Claude Code and Codex follow the same rules.

Nothing else belongs in this file. A rule written here instead of `AGENTS.md`
is a rule Codex never sees.
