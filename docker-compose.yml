services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8080:5173"
    environment:
      VITE_USE_POLLING: "true"
    volumes:
      - .:/app
      - /app/node_modules
